console.log('hello,world');

const element = document.querySelector('h1');
console.log(element);
console.dir(element);

const attr = document.querySelector('#explanation');
console.log(attr);
console.dir(attr);

const guess = document.querySelector('.hint');
console.log(guess);
console.dir(guess);

const list = document.querySelectorAll('p');
console.log(list);

const link = document.querySelectorAll('.example-link');
console.log(link);
